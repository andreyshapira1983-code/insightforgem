{
  "name": "star-galaxy",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format:check": "prettier -c .",
    "format:fix": "prettier -w .",
    "test": "node tests/smoke-diag.js",
    "prepare": "husky install"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.3.3",
    "husky": "^9.0.11",
    "lint-staged": "^15.2.7"
  },
  "lint-staged": {
    "*.{js,css,html,md}": [
      "eslint --fix --max-warnings=0",
      "prettier -w"
    ],
    "*.json": "prettier -w"
  }
}
