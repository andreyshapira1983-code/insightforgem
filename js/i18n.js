// i18n.js (disabled)
// The Star Galaxy website now uses a single language (English). This script
// intentionally does nothing to avoid overriding content with translations.

// When the DOM is ready, do nothing.  This placeholder function prevents
// translation logic from running and ensures the page remains in English.
document.addEventListener('DOMContentLoaded', function () {
  // internationalisation disabled – no translation is applied
});
